# ApexVault-Python (PyQt6 desktop version)
# Core GUI
PyQt6>=6.5

# Face recognition stack (Windows: install via pip wheels)
opencv-python>=4.8
face-recognition>=1.3.0
numpy>=1.24

# Build the Rust core separately:
#   cd apex_crypto && cargo build --release
# then copy apex_crypto.pyd to the project root.
